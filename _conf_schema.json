{
  "render_mode": {
    "description": "查询结果发送方式",
    "hint": "text=纯文字, image=图片渲染, both=同时发送文字和图片",
    "type": "string",
    "default": "text",
    "options": ["text", "image", "both"]
  },
  "groups": {
    "type": "template_list",
    "description": "QQ 群 API 配置列表（每个群独立设置）",
    "hint": "在 Ayabot WebUI → 数据管理 → 开启 API → 复制「API 地址」和「密钥」分别填入下方两项即可。",
    "templates": {
      "group_config": {
        "name": "群配置",
        "display_item": "group_id",
        "hint": "添加一个 QQ 群的 Ayabot 连接",
        "items": {
          "group_id": {
            "description": "QQ 群号",
            "type": "string",
            "default": ""
          },
          "api_url": {
            "description": "API 地址（从 Ayabot 复制）",
            "hint": "从 Ayabot WebUI 数据管理页复制的完整地址，例如 http://host:19810/api/external/user_stats?room_id=xxx",
            "type": "string",
            "default": ""
          },
          "api_token": {
            "description": "API 密钥（从 Ayabot 复制）",
            "hint": "从 Ayabot WebUI 数据管理页复制的密钥",
            "type": "string",
            "default": ""
          }
        }
      }
    }
  },
  "bindings_file": {
    "description": "QQ-UID 绑定数据文件路径（全局通用）",
    "hint": "相对于 AstrBot data 目录",
    "type": "string",
    "default": "ayabot_bindings.json"
  }
}
